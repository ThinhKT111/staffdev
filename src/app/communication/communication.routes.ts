import { Routes } from '@angular/router';

export const COMMUNICATION_ROUTES: Routes = [
  // Các routes sẽ được thêm sau khi tạo components
  { path: '', redirectTo: 'notifications', pathMatch: 'full' }
];